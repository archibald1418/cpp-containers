#include <array>
#include <memory>
#include <random>
//#include <cstring>

// NOTE: compilator should crash with -Werror -std=c++98 -pedantic

int main(){
	std::array <int, 5> a(5);
	std::memmove;
	return (0);	
}
